export class NguoiDung {
  email: string; // Entity + DTO
  name: string; // Entity + DTO
  roles: string[]; // Entity + DTO
  password?: string; // Entity
  enable?: boolean; // Entity
  id?: string; // Entity
}
